# Phase 0 - Foundation dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
loguru==0.7.2
pytest==8.3.2
pytest-asyncio==0.23.8
pre-commit==3.8.0

# Phase 1 - Document Processing & Indexing dependencies
pymupdf==1.26.4  # Updated to match installed version
pdfplumber==0.11.4
rank-bm25==0.2.2
numpy==1.26.4
pandas==2.2.2
rapidfuzz==3.9.6
jsonlines==4.0.0
typer==0.12.3
tiktoken==0.7.0  # For tokenization
rich==13.7.1  # For rich terminal output

# Local embeddings (Phase 1 vector index)
sentence-transformers==3.0.1
# Optional FAISS - uncomment if available in your environment
# faiss-cpu==1.8.0.post1

# Google Gemini SDKs for LLM and embeddings
# Note: Migrated to google-genai for structured output support
# google-generativeai>=0.7.0  # DEPRECATED - Commented out after Phase 0 completion
google-genai>=1.36.0  # Primary SDK for structured output and JSON mode

# Phase 2 - Additional dependencies
faiss-cpu==1.8.0.post1  # Vector search index
openai==1.3.0  # Optional OpenAI support
anthropic==0.7.0  # Optional Anthropic support
tenacity==8.2.3  # Retry logic
transformers==4.36.0  # For cross-encoder reranking
torch>=2.7.0  # Required for transformers and Real-ESRGAN
torchvision>=0.22.0  # Required for Real-ESRGAN
FlagEmbedding>=1.2.0  # Fast BGE reranker with CUDA support

# Phase 4 - Weaviate vector database
weaviate-client>=4.17.0  # Weaviate Python client with v4 API support

# Phase 5 - OpenSearch for BM25 keyword search
opensearch-py==3.0.0  # OpenSearch Python client for remote BM25

# Caching and performance
redis==5.0.1  # For caching
cachetools==5.3.2  # In-memory caching
slowapi==0.1.9  # Rate limiting

# Monitoring and observability
prometheus-client==0.19.0  # Metrics
opentelemetry-api==1.21.0  # Tracing
opentelemetry-sdk==1.21.0  # Tracing SDK
opentelemetry-instrumentation-fastapi==0.42b0  # FastAPI instrumentation

# Week 2 - Resilience patterns
pybreaker==1.0.1  # Circuit breaker pattern for service resilience

# HTTP client used by evaluation runners
requests>=2.31.0

# OCR and advanced document processing
# pytesseract==0.3.13  # DEPRECATED - Replaced by PaddleOCR
# pdfplumber==0.11.4  # Already included above for table extraction

# Google Cloud Vision API for production OCR (replaces PaddleOCR)
google-cloud-vision>=3.7.0  # Document text detection for P&ID and technical drawings

# PaddleOCR - DEPRECATED as of 2025-11-01 - Replaced by Google Cloud Vision API
# Reason: Insufficient accuracy for P&ID tag extraction (90%+ miss rate on instrument tags)
# paddleocr==2.7.3
# paddlepaddle-gpu==2.6.2
# Pillow==10.4.0  # Still needed for image processing
# shapely==2.1.1  # Keep if used elsewhere
# pyclipper==1.3.0.post6  # Keep if used elsewhere

# Image processing (still required)
Pillow==10.4.0

# Real-ESRGAN for OCR image enhancement (Phase 2 - OCR Enhancement)
basicsr>=1.4.2  # Required for Real-ESRGAN
realesrgan>=0.3.0  # AI upscaler for technical drawings
opencv-python>=4.8.0  # Already exists above, listed here for clarity

# Note: These are optional for advanced processing:
# - unstructured==0.15.13 (complex but powerful)
# - unstructured-inference==0.7.36 (layout detection)
# - camelot-py==0.11.0 (requires Ghostscript)
